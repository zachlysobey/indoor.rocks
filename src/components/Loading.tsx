"use client";
import React from "react";

const Loading = () => {
  console.log("called loading");

  return (
    <div
      style={{
        fontSize: "40px",
      }}
    >
      Loading......
    </div>
  );
};

export default Loading;
